# Implementation Plans

Generated by the improve skill on 2026-06-10 at commit `227989b`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the assigned plan fully before starting, honor its STOP conditions, run
all verification gates, and update the status row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Redact subscription paths in audit events | P1 | S | - | DONE |
| 002 | Reject unknown `allowed_request_fields` route keys | P1 | S/M | - | DONE |
| 003 | Bound runtime keyed state for rate limits and write locks | P1 | M | - | DONE |
| 004 | Roll back newly created token files after validation failure | P1 | S | - | DONE |
| 005 | Honor the `reload.sighup` setting | P2 | S/M | - | DONE |
| 006 | Make `allow_version_mismatch` explicit and safe | P2 | M | - | DONE |
| 007 | Add Go vulnerability scanning to local and CI verification | P2 | S | - | DONE |
| 008 | Update restricted-write documentation to match runtime behavior | P2 | S | - | DONE |
| 009 | Split the server hotspot into focused files without behavior changes | P3 | L | 001, 002, 003, 006 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- Plans 001, 002, 003, 004, 005, 006, 007, and 008 are independent and can be executed separately.
- Plan 009 should run after 001, 002, 003, and 006 because all of those plans touch or may touch `internal/server/server.go`; doing the mechanical extraction last avoids needless conflicts.
- Plan 008 is documentation-only and can land anytime, but reviewers should compare it against the final runtime behavior if plans 003 or 006 change write/version semantics.

## Verification baseline

These commands passed during the audit on commit `227989b`:

| Purpose | Command | Result |
|---|---|---|
| Tests | `go test ./...` | passed |
| Race tests | `go test -race ./...` | passed |
| Vet | `go vet ./...` | passed |
| Lint | `golangci-lint run` | passed, 0 issues |
| Coverage smoke | `go test -cover ./...` | passed |
| Route drift | `go run ./cmd/remnaguard routes check-openapi --spec internal/routes/testdata/remnawave-2.7.4-openapi-min.json --strict` | passed, `covered: 185`, `unknown: 0`, `removed: 0`, `coverage: 100%` |

Docker build was not verified locally because the Docker daemon was unavailable in the audit environment.

## Findings considered and rejected

- `allowed_users` not filtering reads: rejected as by-design. `internal/server/server_test.go` has an explicit test named `TestReadWildcardCanReadSafePrivilegedRoutesAndDoesNotUseWriteAllowlistsAsReadFilters`.
- `go list -m -u all` showed available module updates, but no major/EOL migration finding was confirmed. Dependabot already covers Go modules and GitHub Actions weekly.
- Docker context leakage was considered because `Dockerfile` uses `COPY . .`; rejected because `.dockerignore` excludes `.git`, `.github`, `.local`, `.env*`, `tokens.d`, `remnaguard.yaml`, build outputs, and local databases.

## Direction notes not converted to numbered plans

- A richer `remnaguard policy test` dry-run would be useful: current CLI behavior only evaluates route/scope with `policy.Decide`, while real safety also depends on query/body/value constraints. This is a product/DX feature rather than a bug fix; create a separate direction plan if the maintainer wants it.
- Adding `AGENTS.md` would help future agent sessions by documenting repo map, verification gates, and security constraints. This is valuable but not required to close the audited findings above.
